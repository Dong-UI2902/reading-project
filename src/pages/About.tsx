import React, {useEffect} from 'react';

const About = () => {
    useEffect(() => {
        document.title = 'Giới thiệu'
    }, [])

    return (
        <div>

        </div>
    );
};

export default About;