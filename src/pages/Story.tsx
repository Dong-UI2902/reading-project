import React from 'react';
import Cards from "../components/Cards/Cards";

const Story = () => {

    return (
        <div>
            <div>
                <Cards id={'new'} page={'1'}/>
            </div>
        </div>
    );
};

export default Story;