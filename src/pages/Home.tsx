import React from 'react';
import Cards from "../components/Cards/Cards";

const Home = () => {

    return (
        <div>
            <Cards id={'new'} page={'1'} isHome={true}/>
        </div>
    )
};

export default Home;