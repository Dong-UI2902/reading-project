import React from 'react';
import Cards from "../components/Cards/Cards";

const Home = () => {

    return (
        <div>
            <Cards id={'new'}/>
        </div>
    );
};

export default Home;